# Renamed to .gitignore by init.sh — a file named `.gitignore` inside the skill
# would make git ignore the template's own contents.

node_modules/
.next/
out/
build/
next-env.d.ts

.env*
!.env.example

# Review artifacts: screenshots + deterministic layout findings.
.neers/

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.vercel
*.tsbuildinfo
